# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
# don't lint build scripts
build
# don't lint gulpfile
gulpfile.ts
# don't lint vue jsx compat
src/vue-jsx-compat.ts
# don't lint temporary files
temp

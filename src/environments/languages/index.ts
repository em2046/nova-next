export interface Language {
  name: string;
  colorPicker: {
    red: string;
    green: string;
    blue: string;
    alpha: string;
    hue: string;
    saturation: string;
    lightness: string;
  };
}
